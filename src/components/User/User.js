import { Link } from "react-router-dom";

const User = () => {
  return (
    <div>
      <Link className="nav-link" to={"/"}>
        Home
      </Link>
      User component
    </div>
  );
};

export default User;
