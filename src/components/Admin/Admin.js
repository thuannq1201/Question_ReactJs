import { Link } from "react-router-dom";

const Admin = () => {
  return (
    <div>
      <Link className="nav-link" to={"/"}>
        Home
      </Link>
      Admin component
    </div>
  );
};

export default Admin;
